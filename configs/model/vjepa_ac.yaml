# v-jepa-2.1-ac — single-step Action-Conditioned latent predictor (~5M params).
model:
  name: vjepa_ac
  latent_dim: 1024
  action_dim: 2
  hidden_dim: 512
  n_layers: 2
  n_heads: 8
  dropout: 0.0
  predict_residual: true

data:
  horizon: 1
