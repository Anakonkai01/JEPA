# ML / training (the jepa_wm package). Install editable: pip install -e .
torch
torchvision
numpy
opencv-python
pyyaml          # config loading (jepa_wm.utils.config)
tqdm
tensorboard

# Robotics / data-collection rig (robot/). Optional on a pure-training box;
# also installable via:  pip install -e .[robot]
pyserial        # ESP32 serial (robot/capture/recorder.py, robot/tools/*)
pynput          # keyboard input (legacy WASD recorder path)
