# SỔ ĐÊM 2026-06-13→14 — chạy tự chủ (user ngủ, về 10h sáng 06-14)

> **Mục tiêu user:** sáng mai xe **tự chạy được 1 route hoàn chỉnh thật**.
> **Uỷ quyền:** tự chủ toàn bộ (đo/train/eval/thử nghiệm), **làm CẢ HAI** (đường an-toàn CEM+pulse
> tune lại + pivot learned-policy), **tự chọn default theo bằng chứng offline**, **commit + push**.
> Tôi **KHÔNG đụng xe thật** — mọi việc offline. sudo có (không lưu mật khẩu vào file).
> File này = nguồn-sự-thật, cập nhật suốt đêm; là báo cáo sáng. Đọc lại nếu context bị tóm tắt.

## 🔴 STATE / HANDOFF — ĐỌC TRƯỚC (session/account SAU tiếp tục từ đây)
> Phiên trước PAUSE giữa đêm (Claude gần hết limit → user đổi account). File này + `docs/HANDOFF.md`
> = toàn bộ context. **Mọi việc CHÍNH cho sáng mai ĐÃ XONG + commit/push (`3b77169`).**

**ĐÃ XONG (durable, đã push):**
- **PIVOT learned-policy** = lời giải chính. Retrain `policy_prior_cd4` (CPU), validate offline:
  policy-alone bám người lái 0.03 mọi d (CEM xấu dần 0.04→0.17), **sign-match recovery 98%** ở
  full-lock, 1 forward <1ms. Wire `--policy-only` + `run_policy.sh` (STEP=1 validate → LIVE). §2.
- **Đo offline** kết luận: representation TỐT; **lighting MINH OAN** cho quyết-định-lái (nhiễu/tín
  0.10, lật argmin 2-3%); **cosine ≠ cái model dùng** (CEM=patchL1 patch tokens). §0.
- Tooling: `--step` panel (E(steer)+contrast+E(throt)), `run_step.sh`, `meas_tail.py`,
  `eval_policy_bucket.py`, `plot_closed_loop.py`, `probe_energy --frames-dir`.

**CHƯA XONG (KHÔNG chặn sáng — đều là bằng-chứng-phụ/nice-to-have):**
- `meas_tail.py` H2/H4/H5 (đuôi outlier CEM + samples→đuôi + recovery-bucket) — chạy dở bị KILL lúc
  pause. Re-run: `PYTHONPATH=src python scripts/meas_tail.py --n-windows 300 --n-cem 12` (~4 phút;
  H4 với n-cem cao thì RẤT chậm, giữ ≤15). Bổ trợ cho báo cáo, không đổi quyết định.
- H3 cross-session (signal sống điều kiện teach-vs-repeat thật?) — chưa làm.
- Temporal-distance head (thay cosine/GPS-pop) — chưa build (GPS-pop đã chạy, không critical).

**LÀM GÌ TIẾP (nếu còn đêm):** ưu tiên (1) finish `meas_tail` (rẻ), (2) build+train T-dist head
(CPU, research), (3) light eval CEM-vs-policy head-to-head cùng window. **Việc sáng = §4 protocol.**
**⚠ GPU 16GB: chạy 1 job/lần (2 job song song → OOM). Policy train được trên CPU (song song GPU).**

---

## 0) Hiểu biết hội tụ (từ phiên tối nay, có số đo)
- **Representation/world-model ĐỦ TỐT** (offline-win: rollout@1/identity 0.744, action-sens 58/60,
  cross-domain 0.65). Gap KHÔNG ở biểu diễn.
- **Lighting được MINH OAN** cho *quyết-định-lái*: đo 196 window, nhiễu-sáng/tín-hiệu-lái ≈ 0.10,
  lighting lật argmin chỉ 2-3%. (patchL1 goal dịch 0.10 nhưng ~vuông góc → energy đổi ~0.015.)
- **Cosine ANH XEM ≠ cái MODEL dùng.** CEM chấm bằng **patchL1 trên patch tokens**; `centered-cos`
  là pooled-latent, chỉ cho gate nav (đang TẮT: POP=0) + hiển thị. cent-cos nhạy sáng, model thì không.
- **"Chậm thắng" (phát hiện quan trọng nhất của user):** ga↓ → giảm quãng-lái-mù giữa các tick chậm;
  samples↑ → cắt ĐUÔI outlier full-lock (energy phẳng → ít sample bốc ±1). → bài toán **tốc-độ-quyết-
  định**, không phải biểu diễn. Hai lối ra: (a) chấp nhận chậm = pulse/step; (b) learned policy = 1
  forward MLP <1ms = "chính-xác-của-chậm mà chạy nhanh".

## 1b) 🔁 ACCOUNT-2 TIẾP TỤC (06-14, sau pause) — quyết định + sửa quan trọng
> User đổi account, grill xong, chốt: **(1) làm CẢ HAI (floor + upside) time-boxed; (2) GEOSTEER
> BỎ HOÀN TOÀN** (sợ spin như recovery-v1 — không prep, không để trong protocol sáng).
> ⇒ Hệ quả: bỏ geosteer = **mất cơ chế recovery ĐỘC LẬP duy nhất** → recovery (nếu muốn) CHỈ còn
> đường policy. Floor = PURE-VISUAL CEM (sẽ vẫn bung ở cos-dropout NẾU teach lệch buổi → teach
> cùng-buổi + dày là bắt buộc).

**SỬA FLOOR (quan trọng — phiên đêm-1 BỎ SÓT):** `run_infer.sh` đang để **config 06-13 (FAIL ở bãi:
lookahead 2.0 / 128 / pop 0.2 / geosteer)**. Đã REVERT về **config 06-12 tối đợt-2 = lần DUY NHẤT
xác nhận thành công** ("qua cua nhiều run, rất hài lòng"): `256/2 + lookahead 0.5 + pop-confirm 0.5
+ reach 6 + kick 0 + steer-smooth 0.1 + turn-slow 0`, geosteer/xtrack=0, trỏ `policy_prior_cd4` (nhất
quán). Knob env giữ nguyên cho tinh chỉnh ở bãi. `bash -n` OK.

**UPSIDE = RECOVERY-AUGMENTED POLICY (DAVE-2 cho V-JEPA latent, KHÔNG cần GPU):** patch cache là
grid token 24×24 → **dịch ngang grid (border-replicate) = giả "xe lệch làn"** → re-pool → ghép nhãn
**bẻ-về** (shift +s=lệch PHẢI → steer −α·s/W = bẻ TRÁI). 3 script MỚI: `pool_recovery_latents.py`
(sinh latent lệch), `train_policy_recovery.py` (BC trộn p_aug=0.35, alpha=1.0 → `policy_recovery_cd4`),
`eval_recovery_response.py` (**bằng chứng quyết định**: recovery vs baseline có đáp ứng bẻ-về GRADED?).
⚠ **Tín hiệu YẾU** (đo: cos(lệch-trái, lệch-phải) chỉ ~0.94 ở shift 12 → lệch thật cos~0.8 → augment
là PROXY mild) → **transfer chưa chắc**; offline KHÔNG thể chứng minh closed-loop (không renderer).
On-car probe (nhấc xe lệch, đọc dấu steer) = ground truth.

## 1) Kế hoạch đêm + trạng thái (cập nhật account-2)
| # | Việc | Trạng thái |
|---|---|---|
| FLOOR | revert `run_infer.sh` → config 06-12 proven, pure-visual, bỏ geosteer | ✅ XONG (bash -n OK) |
| meas_tail | H2/H4/H5 (đuôi full-lock vs samples → 256/2 có cần không / fast có an toàn?) | ⏳ chạy GPU |
| REC-1 | sinh recovery latent (`pool_recovery_latents.py`, CPU) | ⏳ chạy (~181+28 sess) |
| REC-2 | train `policy_recovery_cd4` (`train_policy_recovery.py`, CPU) | ◻ chờ REC-1 |
| REC-3 | `eval_recovery_response.py` — recovery có bẻ-về graded không (vs baseline)? | ◻ chờ REC-2 |
| H3 | cross-session generalize (goal khác-buổi cùng-chỗ) — nếu còn giờ | ◻ |
| R1 | protocol sáng (bỏ geosteer) + báo cáo + commit/push | ⏳ |
| ~~geosteer~~ | ~~harden/test~~ | ❌ BỎ (user) |
| ~~T-dist~~ | temporal-distance head | ◻ để sau (không critical) |

## 2) Kết quả đo (CẬP NHẬT DẦN)

### ★ POLICY (pivot) — retrain cd4 + validate (CPU, XONG) — KẾT QUẢ MẠNH
`checkpoints/policy_prior_cd4/best.pt` (retrain trên cd4 → split/state-stats nhất quán; bản cũ
`policy_prior/best.pt` train trên WM CŨ → lệch). val w-L1 **0.0699**.

**Action-recovery policy-alone (val, RAW) vs CEM:**
| d | policy \|Δsteer\| | CEM \|Δsteer\| (06-12) |
|---|---|---|
| 1 | 0.028 | 0.038 |
| 2 | 0.028 | 0.098 |
| 4 | 0.030 | 0.132 |
| 8 | **0.031** | **0.172** |
→ policy PHẲNG theo d (CEM xấu dần) → **~5× tốt hơn ở d=8**. Và **1 forward <1ms** vs CEM 0.5–5.5s.

**Recovery theo bucket |teacher steer| (cú bẻ lớn = recovery):**
| bucket \|tea\| | n | \|Δsteer\| | sign-match |
|---|---|---|---|
| thẳng <0.05 | 8231 | 0.014 | — |
| 0.15–0.4 | 2551 | 0.098 | 94% |
| 0.4–0.7 | 1593 | 0.115 | 97% |
| **>0.7 (recovery)** | 2142 | **0.055** | **98%** |
→ policy **TÁI TẠO recovery** (sign 98% ở full-lock), không chỉ giỏi đi thẳng.

⚠ **CAVEAT (phải nhớ):** đều là val **IN-DISTRIBUTION** (goal = tương lai CÙNG traj). Closed-loop
cross-session (context live khác buổi + off-line) + generalize **CHƯA kiểm offline** (cần xe/sim).
BC có recovery-data nên off-line→về CÓ trong train, nhưng chưa chứng minh generalize.

### Wiring + launcher (XONG, compile OK)
- `--policy-only` trong `inference_loop.py`: action = policy TRỰC TIẾP (bỏ CEM), vẫn qua smoothing/
  floors/cap. Default TẮT (đường CEM an toàn nguyên).
- `run_policy.sh`: `STEP=1 bash run_policy.sh` (validate tay) → `bash run_policy.sh` (LIVE 5Hz).
- `--step` panel (`run_step.sh`) đã nâng cấp (E(steer)+contrast+E(throt)) từ phiên tối.

### H2/H4/H5 (meas_tail, XONG 06-14 account-2) — d=4, 300 window VAL frozen
**★ H4 (PHÁT HIỆN — bác bỏ giả thuyết đêm-1 "nhiều-sample cắt đuôi"):** samples 16→256 KHÔNG
giảm phương sai argmin (~0.18–0.23, PHẲNG) cũng KHÔNG giảm %full-lock đoạn-thẳng (~14–16%, PHẲNG).
→ **256/2 KHÔNG hơn 64/2 về chất lượng** → dùng **64/2 (1.6s tick) thay 256/2 (5.5s)** = ít lái-mù
hơn 3.4×. Thành công 06-12 là nhờ **ga THẤP + teach dày + lookahead ngắn**, KHÔNG phải nhiều-sample.
- H2: thẳng (n=198) chỉ **3%** argmin biên full-lock (sạch); cua (n=102) **31%** (phần lớn hợp lệ).
  Đuôi full-lock ~14-16% là **NỘI TẠI world-model** (samples không sửa được) → smoothing + lookahead ngắn.
- H5: argminE đúng dấu **94-100%** ở cua (model BIẾT hướng cua — nhưng in-dist, KHÔNG phải lệch-làn-recovery).
→ **Ý nghĩa floor:** giữ 256/2 làm default proven (an toàn), NHƯNG `SMP=64` là knob đầu nên thử (offline ≈,
nhanh hơn → closed-loop tốt hơn). Ý nghĩa upside: trần chất-lượng CEM ~cố định → policy (1 forward) bám
được CEM ở 5Hz là hấp dẫn.
### REC-3 baseline CONTROL (eval_recovery_response, baseline policy_prior_cd4, 6000 val anchor)
**Baseline ĐÃ có recovery YẾU nhưng ĐÚNG:** đáp ứng với lệch tổng hợp là monotone + đúng dấu
(shift −12=lệch trái → steer **+0.065** phải; shift +12=lệch phải → **−0.087** trái), **slope −0.0065**.
→ pooled latent CÓ mang tín hiệu lệch→bẻ-về (goal-conditioning cho recovery ngầm), chỉ **YẾU**
(Δ≤0.09). ⇒ Cơ chế ĐÚNG (không phải mù recovery như tưởng). **Việc của augment = KHUẾCH ĐẠI slope**
thành recovery dùng được. **Tiêu chí PASS REC-3:** recovery slope dốc hơn rõ baseline (−0.0065) +
vẫn monotone + |Δ| ở shift 12 đủ lớn (vd ≥0.25) để kéo về thật. Rủi ro còn lại = transfer synthetic→thật.
### H3 cross-session — *(chờ, nếu còn giờ)*

## 3) QUYẾT ĐỊNH (dựa bằng chứng offline đêm nay)

**Chọn cho sáng (account-2, geosteer BỎ): DEFAULT = FLOOR CEM proven (`run_infer.sh`) — đây là
con đường DUY NHẤT đã xác nhận thành công thật (06-12). Recovery-policy = UPSIDE có cổng (chỉ
promote nếu (a) offline `eval_recovery_response` cho đáp ứng bẻ-về GRADED + (b) on-car probe đúng dấu).**

Lý do đảo so với đêm-1: (1) bỏ geosteer = pure-visual KHÔNG có recovery độc lập → không dám đặt
cược mù vào policy; (2) "recovery 98%" đêm-1 là cua-bình-thường in-dist, KHÔNG phải lệch-làn-bẻ-về;
(3) floor 06-12 là bằng chứng cứng duy nhất. Recovery-policy mới (augment) tấn công đúng gốc nhưng
augment pooled-latent là PROXY yếu → phải qua 2 cổng trước khi tin.

## 4) ▶️ GIAO CHO 10H SÁNG — PROTOCOL CHẠY ROUTE (geosteer ĐÃ BỎ — pure-visual)

**B0. Re-teach route CÙNG BUỔI (BẮT BUỘC — pure-visual không có recovery, teach là tất cả):**
lái 1 lap CH10 REC → `PYTHONPATH=src python scripts/route_from_session.py <session> <tên> --step-m 0.35`
(tự dày ở cua), HOẶC teach live. Khác buổi → ccos sập → pop miss → bung.

**B1. ⭐ FLOOR = CHẠY NGAY (proven, default):** `bash run_infer.sh`
- = CEM 256/2 pure-visual, config 06-12 đợt-2 (lần "rất hài lòng"). Ngón tay trên STOP (web ⛔ / CH9 manual).
- Knob bãi (env): `THR=0.08` chậm lại · `LOOK=0.6` nếu quẹo muộn · `POP=0` nếu pop kẹt · `KICK=0.10` nếu đề-pa kẹt cua.
- Đây là con đường để **hoàn thành route**. Bung ở cua/cos-dropout → teach DÀY hơn (`--step-m 0.25`) + `THR` thấp.

**B2. UPSIDE = recovery-policy (CHỈ nếu đêm offline PASS — xem verdict §2 "REC-3"):** `STEP=1 bash run_policy.sh`
- Verdict offline recovery: **(điền sau khi `eval_recovery_response` xong — nếu slope<0 + monotone thì THỬ; nếu phẳng thì BỎ, ở lại B1).**
- ⚠ Đổi `run_policy.sh` trỏ `checkpoints/policy_recovery_cd4/best.pt` (xem §2) trước khi chạy.
- **PROBE RECOVERY on-car (ground truth):** đặt xe ON-line → `s` (frame, không chạy) → `model@` ~0?
  Nhấc xe lệch ~1m TRÁI → `s` → `model@` có **+ (bẻ phải về line)**? lệch PHẢI → **− (bẻ trái)**?
  CÓ + đúng dấu → recovery chạy thật → chạy live `bash run_policy.sh`. KHÔNG/sai dấu → BỎ, về B1.

**Files/ckpt:** `run_infer.sh` (FLOOR, đã trỏ `policy_prior_cd4` cho warm-start). `run_policy.sh`
(UPSIDE, --policy-only). `run_step.sh` (CEM gate tay nếu muốn soi). Recovery ckpt `policy_recovery_cd4`
+ baseline `policy_prior_cd4` gitignored → máy khác retrain: `pool_recovery_latents.py` rồi
`train_policy_recovery.py` (~vài phút CPU; cần patch cache `data/latents_*_patch_384`).

**RỦI RO:** (1) **pure-visual KHÔNG recovery** (geosteer bỏ) → teach cùng-buổi + DÀY là phòng-thủ
DUY NHẤT chống bung; (2) recovery-policy là augment-PROXY chưa kiểm xe → B2 probe là cổng chặn, sai
dấu thì bỏ; (3) "ga thấp không lăn" (ma-sát-tĩnh) → `KICK=0.10`/`THR` lên nếu đơ, xuống nếu giật.
